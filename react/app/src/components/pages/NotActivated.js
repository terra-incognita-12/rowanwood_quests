import Typography from '@mui/material/Typography';

const NotActivated = () => {
	return (
		<Typography className="mt-4" variant="h3" display="flex" justifyContent="center" alignItems="center">This Quest is not activated yet.</Typography>
	)
}

export default NotActivated